<div class="cta-section bg-gradient-to-r from-indigo-600 to-purple-600 rounded-2xl p-10 text-center my-16">
  <h2 class="text-3xl font-bold mb-4 text-white">{{ $title }}</h2>
  <p class="text-xl max-w-2xl mx-auto mb-8 text-indigo-100">
    {{ $subtitle }}
  </p>
  <a href="{{ $button_link }}" class="btn-neon bg-white text-indigo-600 hover:bg-indigo-50 px-8 py-4 text-lg font-medium">
    {{ $button_text }}
  </a>
</div>
